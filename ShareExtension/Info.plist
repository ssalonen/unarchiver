<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleName</key>
    <string>ShareExtension</string>
    <key>CFBundleDisplayName</key>
    <string>UnArchiver</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>CFBundlePackageType</key>
    <string>XPC!</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>NSExtension</key>
    <dict>
        <key>NSExtensionPointIdentifier</key>
        <string>com.apple.share-services</string>
        <key>NSExtensionPrincipalClass</key>
        <string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
        <key>NSExtensionAttributes</key>
        <dict>
            <!-- Predicate-based rule so iOS shows this app in the share sheet
                 for the specific archive UTIs we support (including zip). -->
            <key>NSExtensionActivationRule</key>
            <string>SUBQUERY(extensionItems, $extensionItem,
  SUBQUERY($extensionItem.attachments, $attachment,
    ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.zip-archive"   ||
    ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "com.pkware.zip-archive" ||
    ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.tar-archive"   ||
    ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "org.gnu.gnu-tar-gzip-archive" ||
    ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.gzip-archive"  ||
    ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "org.gnu.gnu-zip-archive" ||
    ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "org.bzip2.bzip2-archive" ||
    ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.bzip2-archive" ||
    ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "org.tukaani.xz-archive"
  ).@count > 0
).@count > 0</string>
        </dict>
    </dict>
</dict>
</plist>
